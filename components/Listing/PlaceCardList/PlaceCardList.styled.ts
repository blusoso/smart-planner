import { ScrollHorizontal } from "../../Mixin/Mixin.styled";
import styled from "styled-components";

export const PlaceCardListContainer = styled(ScrollHorizontal)`
  margin-right: -1.2em;
  position: relative;
  display: inline-flex;
  gap: 1em;
`;
