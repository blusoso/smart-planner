import { ScrollHorizontal } from "../../Mixin/Mixin.styled";
import styled from "styled-components";

export const PlaceCardListScroll = styled(ScrollHorizontal)`
  margin-right: -1.2em;
`;

export const PlaceCardListContainer = styled.div`
  position: relative;
  display: inline-flex;
  gap: 1em;
`;
